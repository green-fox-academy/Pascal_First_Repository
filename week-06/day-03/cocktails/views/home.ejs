<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Cocktails</title>
    <link rel="stylesheet" type="text/css" href="style.css">

</head>

<body class="body">

    <h1 class="title">Cocktails</h1>

    <nav>
        <a href="/">all</a>
        <% for (let i = 0; i < alcoholList.length; i++){ %>
        <a href="/?alcohol=<%= alcoholList[i] %>"> <%= alcoholList[i] %></a> <% } %>
    </nav>

    <ul>

        <li>
            <div class="name header">name</div>
            <div class="price header">price</div>
        </li>

        <% for (let i = 0; i < cocktails.length; i++) {%>
        <% let isGrey = i % 2 === 0 %>

        <li class="main">
            <div class="name <%= `${isGrey}`%>">
                <%= cocktails[i].name%>
                <% if(cocktails[i].isAlcoholic === false) {%>
                <span>(non-alcoholic)</span>
                <% }%>
                <div class=ingredients>
                    <%= cocktails[i].contains.join(' ') %></div>
            </div>
            <div class="price <%= `${isGrey}`%>"">
            <%= cocktails[i].price%> Ft
        </div>
    </li>
<%}%>

</ul>
</body>

</html>