<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Cocktails</title>
</head>

<body>
    <ul>
        <% if (cocktails.length !== 0) { %>
        <% for (let i = 0; i < cocktails.length; i++) {%>
        <li><%= cocktails[i].name%></li>
        <% }} else {%>
        <h1><%= 'Go drink somewhere else!'%></h1>
        <% } %></ul>

</body>

</html>